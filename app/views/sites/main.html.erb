
<div class="callout large">
  <div class="row column text-center">
    <h1>Tagger le web pour mavoix</h1>
    <p class="lead">Commenter et liker les commentaires de notre communauté</p>
  </div>
</div>

<table>
  <thead>
    <tr>
      <th>Url</th>
      <th colspan="3"></th>
    </tr>
  </thead>



  <p> vous êtes identifié avec votre ip afin d'eviter un system de login</p>
  <div class="circlenumber">
        vous avez liké
        <h3 id="nb_like" ><%=@checked_site.length.to_s%></h3>
  </div>
  <p> vous êtes: -<%=@user.ip%>-</p>
<div class="large-12 columns ">
  <p id="notice" class= "alert-box round " style="display:none;"><%= notice %></p>
</div>

  <tbody id="all_site">
      <tr>
        <td class="field">
          <input type="text" name="site[url]" id="new_site_url">
        </td>
        <td class="actions">
          <div id="new_site_url_button" class="button">Je l'ai commenté</div>
        </td>
      </tr>
    <% @sites.each do |site| %>
      <tr>
        <% if site.users.include? @user %>
          <td><%= site.url %></td>
          <td></td>
        <% else%>
          <td><a target="_blank" href="<%= site.url %>"><%= site.url %></a></td>
          <td>
            <div type="new_checkin" class="button" url="<%=checkin_site_path({:id => site})%>">
            "je vais le liké"
            </div>
        <%end%>
      </tr>
    <% end %>
  </tbody>
</table>

